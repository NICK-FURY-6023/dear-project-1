# """
# Django settings for lxfpro project.

# Generated by 'django-admin startproject' using Django 4.2.

# For more information on this file, see
# https://docs.djangoproject.com/en/4.2/topics/settings/

# For the full list of settings and their values, see
# https://docs.djangoproject.com/en/4.2/ref/settings/
# """

# from pathlib import Path

# # Build paths inside the project like this: BASE_DIR / 'subdir'.
# BASE_DIR = Path(__file__).resolve().parent.parent
# BASE_URL = 'http://127.0.0.1:8000'  # Use your local development server URL


# # Quick-start development settings - unsuitable for production
# # See https://docs.djangoproject.com/en/4.2/howto/deployment/checklist/

# # SECURITY WARNING: keep the secret key used in production secret!
# SECRET_KEY = 'django-insecure-s*y$&q+j(2&8=mbs(!hf1mac6@g0#bo^a#p8%-=fusfi@n2wh-'

# # SECURITY WARNING: don't run with debug turned on in production!
# DEBUG = True

# ALLOWED_HOSTS = ['127.0.0.1', 'localhost']


# # Application definition

# INSTALLED_APPS = [
#     'django.contrib.admin',
#     'django.contrib.auth',
#     'django.contrib.contenttypes',
#     'django.contrib.sessions',
#     'django.contrib.messages',
#     'django.contrib.staticfiles',
#     'accounts',
#     'found_app',
#     'channels',
#     'user',
#     'chat',
    
# ]

# MIDDLEWARE = [
#     'django.middleware.security.SecurityMiddleware',
#     'django.contrib.sessions.middleware.SessionMiddleware',
#     'django.middleware.common.CommonMiddleware',
#     'django.middleware.csrf.CsrfViewMiddleware',
#     'django.contrib.auth.middleware.AuthenticationMiddleware',
#     'django.contrib.messages.middleware.MessageMiddleware',
#     'django.middleware.clickjacking.XFrameOptionsMiddleware',
# ]

# ROOT_URLCONF = 'lxfpro.urls'

# TEMPLATES = [
#     {
#         'BACKEND': 'django.template.backends.django.DjangoTemplates',
#         'DIRS': [],
#         'APP_DIRS': True,
#         'OPTIONS': {
#             'context_processors': [
#                 'django.template.context_processors.debug',
#                 'django.template.context_processors.request',
#                 'django.contrib.auth.context_processors.auth',
#                 'django.contrib.messages.context_processors.messages',
#             ],
#         },
#     },
# ]

# WSGI_APPLICATION = 'lxfpro.wsgi.application'
# ASGI_APPLICATION = 'lxfpro.asgi.application'


# # Database
# # https://docs.djangoproject.com/en/4.2/ref/settings/#databases

# DATABASES = {
#    'default': {
#         'ENGINE': 'django.db.backends.mysql',
#         'NAME': 'lxf2',
#         'USER': 'root',
#         'PASSWORD': '',
#         'PORT': '3306',
#         'HOST': 'localhost',
#     }
# }

# # CHANNEL_LAYERS configuration for Redis
# CHANNEL_LAYERS = {
#     "default": {
#         "BACKEND": "channels.layers.InMemoryChannelLayer",
#     },
# }




# EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
# EMAIL_HOST = 'smtp.gmail.com'  
# EMAIL_PORT = 587  
# EMAIL_USE_TLS = True  
# EMAIL_HOST_USER = 'lostandfound.vpmrzshah@gmail.com'  # Replace with your sender Gmail  
# EMAIL_HOST_PASSWORD = 'olcn hlep cryq bvag'  # Use the generated app password, NOT your Gmail password  
# DEFAULT_FROM_EMAIL = EMAIL_HOST_USER  


# # Password validation
# # https://docs.djangoproject.com/en/4.2/ref/settings/#auth-password-validators

# AUTH_PASSWORD_VALIDATORS = [
#     {
#         'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
#     },
#     {
#         'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
#     },
#     {
#         'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
#     },
#     {
#         'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
#     },
# ]


# # Internationalization
# # https://docs.djangoproject.com/en/4.2/topics/i18n/

# LANGUAGE_CODE = 'en-us'

# TIME_ZONE = 'UTC'

# USE_I18N = True

# USE_TZ = True


# # Static files (CSS, JavaScript, Images)
# # https://docs.djangoproject.com/en/5.1/howto/static-files/

# STATIC_URL = '/static/'

# # Default primary key field type
# # https://docs.djangoproject.com/en/5.1/ref/settings/#default-auto-field

# DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

# # Session settings to ensure persistence

# # Use database-backed sessions
# SESSION_ENGINE = 'django.contrib.sessions.backends.db'

# # Session will expire when the user closes the browser if False
# SESSION_EXPIRE_AT_BROWSER_CLOSE = False

# # Session lifetime (in seconds) - 2 weeks
# SESSION_COOKIE_AGE = 1209600  # Two weeks

# # Secure cookies settings (adjust based on environment)
# SESSION_COOKIE_SECURE = False  # Set to True if using HTTPS
# CSRF_COOKIE_SECURE = False  # Set to True if using HTTPS

# import logging

# LOGGING = {
#     'version': 1,
#     'disable_existing_loggers': False,
#     'handlers': {
#         'console': {
#             'class': 'logging.StreamHandler',
#         },
#     },
#     'root': {
#         'handlers': ['console'],
#         'level': 'DEBUG',
#     },
# }

# STATIC_URL = '/static/'
# STATICFILES_DIRS = [BASE_DIR / "static"]

# MEDIA_URL = '/media/'
# MEDIA_ROOT = BASE_DIR / 'media'


"""
Django settings for lxfpro project - PRODUCTION READY
"""

from pathlib import Path
import os

# Build paths
BASE_DIR = Path(__file__).resolve().parent.parent

# SECURITY WARNING: Change this in production!
SECRET_KEY = os.environ.get('DJANGO_SECRET_KEY', 'django-insecure-s*y$&q+j(2&8=mbs(!hf1mac6@g0#bo^a#p8%-=fusfi@n2wh-')

# SECURITY WARNING: Don't run with debug=True in production!
DEBUG = os.environ.get('DEBUG', 'True') == 'True'

ALLOWED_HOSTS = ['127.0.0.1', 'localhost', '*']  # Configure for production domain

BASE_URL = os.environ.get('BASE_URL', 'http://127.0.0.1:8000')

# Application definition
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'accounts',
    'found_app',
    'channels',
    'user',
    'chat',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'lxfpro.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'lxfpro.wsgi.application'
ASGI_APPLICATION = 'lxfpro.asgi.application'

# Database - SQLite (Production-Ready Configuration)
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
        'OPTIONS': {
            'timeout': 20,  # Prevent database locked errors
        }
    }
}

# For Production: Switch to PostgreSQL/MySQL
# DATABASES = {
#     'default': {
#         'ENGINE': 'django.db.backends.postgresql',
#         'NAME': 'lostfound_production',
#         'USER': 'your_username',
#         'PASSWORD': 'your_password',
#         'HOST': 'your-postgres-host.com',
#         'PORT': '5432',
#         'OPTIONS': {
#             'connect_timeout': 10,
#         }
#     }
# }

# CHANNEL_LAYERS configuration for Redis (currently in-memory)
CHANNEL_LAYERS = {
    "default": {
        "BACKEND": "channels.layers.InMemoryChannelLayer",
    },
}

# Email settings
# DUAL MODE - Sends emails via Gmail AND prints to console (Best for demo!)
EMAIL_BACKEND = 'lxfpro.email_backend.DualEmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'lostandfound.vpmrzshah@gmail.com'
EMAIL_HOST_PASSWORD = 'olcnhlepcryqbvag'  # Gmail App Password (removed spaces)
DEFAULT_FROM_EMAIL = EMAIL_HOST_USER

# Other options (switch karne ke liye):
# Console Only: EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'
# SMTP Only: EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'

# Password validation
AUTH_PASSWORD_VALIDATORS = [
    {'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator'},
    {'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator'},
    {'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator'},
    {'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator'},
]

# Internationalization
LANGUAGE_CODE = 'en-us'
TIME_ZONE = 'UTC'
USE_I18N = True
USE_TZ = True

# Static files
STATIC_URL = '/static/'
STATICFILES_DIRS = [BASE_DIR / "static"]

# Media files
MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'

# Default primary key field type
DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

# Session settings
SESSION_ENGINE = 'django.contrib.sessions.backends.db'
SESSION_EXPIRE_AT_BROWSER_CLOSE = False
SESSION_COOKIE_AGE = 1209600  # Two weeks
SESSION_COOKIE_SECURE = False
CSRF_COOKIE_SECURE = False

# CSRF Trusted Origins - For external access
CSRF_TRUSTED_ORIGINS = [
    'http://localhost:8000',
    'http://127.0.0.1:8000',
    'https://*.github.dev',  # GitHub Codespaces
    'https://*.ngrok-free.app',  # ngrok
    'https://*.ngrok.io',  # ngrok legacy
    'https://*.up.railway.app',  # Railway
    'https://*.onrender.com',  # Render
    'https://*.pythonanywhere.com',  # PythonAnywhere
]

# Logging
import logging
LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'handlers': {'console': {'class': 'logging.StreamHandler'}},
    'root': {'handlers': ['console'], 'level': 'DEBUG'},
}

# Authentication URLs
LOGIN_URL = '/login/'
LOGIN_REDIRECT_URL = '/'
LOGOUT_REDIRECT_URL = '/login/'
